# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET     /adm                        controllers.Application.adm()
GET     /user                       controllers.Application.user()
POST	/adicionaEvento				controllers.Application.adicionaEvento()
POST	/loginAdm					controllers.Application.index()
POST	/cadastrar					controllers.Application.cadastro()
POST	/cadastro					controllers.Application.adicionaParticipante()
POST	/filtrarUser				controllers.Application.filtrarUser()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
